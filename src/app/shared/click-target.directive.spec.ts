import { ClickTargetDirective } from './click-target.directive';

describe('ClickTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickTargetDirective();
    expect(directive).toBeTruthy();
  });
});
